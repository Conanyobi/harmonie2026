---
title: "RAPPORT D'ANALYSE DESCRIPTIVE"
subtitle: "Mission HARMONIE 2026"
date: today
lang: fr
format:
  docx:
    toc: false
    number-sections: true
    fig-width: 8
    fig-height: 6
  html:
    toc: true
    toc-depth: 3
    code-fold: true
    theme: cosmo
    fig-width: 8
    fig-height: 6
execute:
  echo: false
  warning: false
  message: false
---

```{r setup}
#| include: false

knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE,
                      fig.width = 8, fig.height = 6)

library(dplyr)
library(knitr)
library(flextable)
library(officer)

set_flextable_defaults(
  font.family = "Arial",
  font.size = 9,
  theme_fun = "theme_booktabs"
)

format_number <- function(x, digits = 1) {
  if (length(x) == 0 || all(is.na(x))) return("-")
  format(round(x, digits), nsmall = digits, decimal.mark = ",",
         big.mark = " ", scientific = FALSE, trim = TRUE)
}

clean_label <- function(x) {
  x <- gsub("_", " ", x)
  x <- gsub("\\s+", " ", x)
  trimws(x)
}

var_labels <- c(
  DATE_RECENSEMENT = "Date de recensement",
  SEXE = "Sexe",
  AGE = "Âge (années)",
  CLASSE_AGE = "Classe d'âge",
  LIEU_RESIDENCE = "Lieu de résidence",
  ETABLISSEMENT_DE_SOINS_HABITUE = "Établissement de soins habituel",
  CATEGORIE = "Catégorie du patient",
  UNITE_CORPS_STRUCTURE_SANITAIR = "Unité / corps / structure sanitaire",
  DOSSIER_MEDICAL = "Disponibilité du dossier médical",
  MOTIF_DIAGNOSTIC = "Motif / diagnostic",
  AUTRES_SPECIALITES = "Autres spécialités demandées",
  ATCD_ALLERGIES_TRAITEMENTS = "Antécédents, allergies ou traitements",
  ETAT_GL_STABLE = "État général stable",
  TA = "Tension artérielle (saisie libre)",
  PATIENT_RECU_INFO = "Information reçue par le patient",
  CONSENTEMENT = "Consentement",
  PEUT_FINANCER_BILAN_PREOP = "Capacité à financer le bilan préopératoire",
  JOUR_PROGRAMME = "Jour programmé",
  Structure = "Code structure"
)

get_label <- function(v) {
  if (v %in% names(var_labels)) unname(var_labels[[v]]) else clean_label(v)
}

style_ft <- function(ft, first_col_width = 3.8) {
  ft %>%
    align(align = "center", part = "all") %>%
    align(j = 1, align = "left", part = "body") %>%
    bold(part = "header") %>%
    font(fontname = "Arial", part = "all") %>%
    fontsize(size = 9, part = "all") %>%
    theme_vanilla() %>%
    border_remove() %>%
    hline_top(border = fp_border(color = "#333333", width = 1.2), part = "header") %>%
    hline_bottom(border = fp_border(color = "#333333", width = 1.2), part = "header") %>%
    hline_bottom(border = fp_border(color = "#333333", width = 1.2), part = "body") %>%
    width(j = 1, width = first_col_width) %>%
    autofit() %>%
    fit_to_width(max_width = 6.5)
}

print_ft <- function(ft) {
  cat(flextable_to_rmd(ft))
  cat("\n\n&nbsp;\n\n")
}

cat_table <- function(data, var, title = NULL, include_missing = TRUE) {
  if (!var %in% names(data)) return(invisible(NULL))
  if (is.null(title)) title <- paste("Répartition selon", tolower(get_label(var)))
  cat("\n\n**", title, "**\n\n", sep = "")

  x <- data[[var]]
  x <- ifelse(is.na(x) | trimws(as.character(x)) == "", "Non renseigné", as.character(x))
  tab <- as.data.frame(table(x, useNA = "no"), stringsAsFactors = FALSE)
  names(tab) <- c("Modalité", "Effectif")
  if (!include_missing) tab <- tab[tab$Modalité != "Non renseigné", , drop = FALSE]
  denom <- sum(tab$Effectif)
  tab$Pourcentage <- if (denom > 0) paste0(format_number(100 * tab$Effectif / denom, 1), " %") else "-"
  tab <- tab[order(-tab$Effectif, tab$Modalité), , drop = FALSE]
  total <- data.frame(Modalité = "TOTAL", Effectif = sum(tab$Effectif), Pourcentage = "100,0 %")
  tab <- rbind(tab, total)

  ft <- flextable(tab)
  ft <- style_ft(ft)
  ft <- bold(ft, i = nrow(tab), bold = TRUE, part = "body")
  print_ft(ft)
}

num_table <- function(data, var, title = NULL) {
  if (!var %in% names(data)) return(invisible(NULL))
  x <- suppressWarnings(as.numeric(data[[var]]))
  x <- x[is.finite(x)]
  if (is.null(title)) title <- paste("Description de", tolower(get_label(var)))
  cat("\n\n**", title, "**\n\n", sep = "")

  if (length(x) == 0) {
    tab <- data.frame(Statistique = "Données disponibles", Valeur = "0")
  } else {
    tab <- data.frame(
      Statistique = c("Effectif renseigné", "Moyenne", "Écart-type", "Médiane", "Minimum", "Maximum"),
      Valeur = c(length(x), format_number(mean(x), 1), format_number(sd(x), 1),
                 format_number(median(x), 1), format_number(min(x), 1), format_number(max(x), 1))
    )
  }
  print_ft(style_ft(flextable(tab), first_col_width = 3.2))
}

multiresponse_table <- function(data, vars, labels, title, denominator = nrow(data)) {
  vars <- vars[vars %in% names(data)]
  if (length(vars) == 0) return(invisible(NULL))
  labels <- labels[match(vars, names(labels))]
  eff <- sapply(vars, function(v) sum(suppressWarnings(as.numeric(data[[v]])) == 1, na.rm = TRUE))
  tab <- data.frame(
    Modalité = unname(labels),
    Effectif = as.integer(eff),
    Pourcentage = paste0(format_number(100 * eff / denominator, 1), " %"),
    check.names = FALSE
  )
  tab <- tab[order(-tab$Effectif, tab$Modalité), , drop = FALSE]
  cat("\n\n**", title, "**\n\n", sep = "")
  print_ft(style_ft(flextable(tab), first_col_width = 4.4))
  cat("*Note : réponses multiples possibles ; les pourcentages sont calculés sur l'ensemble des patients analysés.*\n\n")
}
```

```{r load-data}
# Lecture des données brutes de la mission
raw <- read.csv("data/Mission_HARMONIE_2026.csv", stringsAsFactors = FALSE,
                check.names = FALSE, na.strings = c("", "NA"))

# Retirer les lignes entièrement vides (une ligne vide peut être présente à l'export)
data <- raw[rowSums(!is.na(raw)) > 0, , drop = FALSE]

# Dates et variables dérivées utiles à la description
parse_mdy <- function(x) as.Date(x, format = "%m-%d-%y")
data$DATE_RECENSEMENT_DATE <- parse_mdy(data$DATE_RECENSEMENT)
data$DATE_NAISSANCE_DATE <- parse_mdy(data$DATE_NAISSANCE)

data$AGE <- floor(as.numeric(data$DATE_RECENSEMENT_DATE - data$DATE_NAISSANCE_DATE) / 365.2425)
data$AGE[data$AGE < 0 | data$AGE > 120] <- NA

data$CLASSE_AGE <- cut(
  data$AGE,
  breaks = c(-Inf, 17, 29, 39, 49, 59, Inf),
  labels = c("< 18 ans", "18-29 ans", "30-39 ans", "40-49 ans", "50-59 ans", "60 ans et plus"),
  right = TRUE
)

# Libellés plus lisibles pour quelques variables codées
data$SEXE <- recode(tolower(trimws(data$SEXE)), "f" = "Féminin", "m" = "Masculin", .default = data$SEXE)
data$CATEGORIE <- recode(data$CATEGORIE,
                         "civil" = "Civil", "ayant_droit" = "Ayant droit",
                         "gendarme" = "Gendarme", .default = data$CATEGORIE)
data$DOSSIER_MEDICAL <- recode(data$DOSSIER_MEDICAL,
                               "photographie_et_joint" = "Photographié et joint",
                               "non_disponible" = "Non disponible",
                               .default = data$DOSSIER_MEDICAL)
data$PEUT_FINANCER_BILAN_PREOP <- recode(data$PEUT_FINANCER_BILAN_PREOP,
                                         "oui" = "Oui", "non" = "Non",
                                         "en_partie" = "En partie",
                                         .default = data$PEUT_FINANCER_BILAN_PREOP)
data$CONSENTEMENT <- recode(data$CONSENTEMENT, "oui" = "Oui", "non" = "Non", .default = data$CONSENTEMENT)
data$ETAT_GL_STABLE <- recode(data$ETAT_GL_STABLE, "oui" = "Oui", "non" = "Non", .default = data$ETAT_GL_STABLE)
```

# STATISTIQUE DESCRIPTIVE

Ce rapport présente uniquement des analyses descriptives. Les variables directement identifiantes (nom, prénoms, téléphone, contact proche, identifiants techniques, pièces jointes et coordonnées de l'enregistreur) ne sont pas affichées dans les tableaux.

## 1. Vue d'ensemble de la base

```{r overview}
#| results: asis

n_patients <- nrow(data)
n_vars <- ncol(raw)
n_consent <- if ("CONSENTEMENT" %in% names(data)) sum(data$CONSENTEMENT == "Oui", na.rm = TRUE) else NA

resume <- data.frame(
  Indicateur = c("Patients analysés", "Variables dans le fichier source", "Consentements 'Oui'"),
  Valeur = c(n_patients, n_vars, n_consent)
)
print_ft(style_ft(flextable(resume), first_col_width = 4.2))

cat_table(data, "DATE_RECENSEMENT", "Répartition des patients selon la date de recensement")
```

## 2. Caractéristiques des patients

```{r patient-characteristics}
#| results: asis

cat_table(data, "SEXE")
num_table(data, "AGE")
cat_table(data, "CLASSE_AGE")
cat_table(data, "CATEGORIE")
cat_table(data, "LIEU_RESIDENCE")
cat_table(data, "ETABLISSEMENT_DE_SOINS_HABITUE")
cat_table(data, "UNITE_CORPS_STRUCTURE_SANITAIR")
cat_table(data, "Structure", "Répartition selon le code de structure")
```

## 3. Situation clinique et dossier médical

```{r clinical}
#| results: asis

cat_table(data, "DOSSIER_MEDICAL")
cat_table(data, "ETAT_GL_STABLE")
cat_table(data, "MOTIF_DIAGNOSTIC")
cat_table(data, "ATCD_ALLERGIES_TRAITEMENTS")
cat_table(data, "TA")
```

## 4. Spécialités demandées

```{r specialties}
#| results: asis

specialty_labels <- c(
  "Sp_cialit_demand_e_/cardiologie" = "Cardiologie",
  "Sp_cialit_demand_e_/pneumologie" = "Pneumologie",
  "Sp_cialit_demand_e_/gastro_ent_rologie" = "Gastro-entérologie",
  "Sp_cialit_demand_e_/n_phrologie" = "Néphrologie",
  "Sp_cialit_demand_e_/neurologie" = "Neurologie",
  "Sp_cialit_demand_e_/endocrinologie" = "Endocrinologie",
  "Sp_cialit_demand_e_/chirurgie_g_n_rale" = "Chirurgie générale",
  "Sp_cialit_demand_e_/orthop_die" = "Orthopédie",
  "Sp_cialit_demand_e_/urologie" = "Urologie",
  "Sp_cialit_demand_e_/chirurgie_des_br_l_s" = "Chirurgie des brûlés",
  "Sp_cialit_demand_e_/opthalmologie" = "Ophtalmologie",
  "Sp_cialit_demand_e_/orl" = "ORL",
  "Sp_cialit_demand_e_/odontologie" = "Odontologie",
  "Sp_cialit_demand_e_/gyn_cologie_obst_trique" = "Gynécologie-obstétrique",
  "Sp_cialit_demand_e_/p_diatrie" = "Pédiatrie",
  "Sp_cialit_demand_e_/dermatologie" = "Dermatologie",
  "Sp_cialit_demand_e_/m_decine_traditionnelle_chinoise" = "Médecine traditionnelle chinoise",
  "Sp_cialit_demand_e_/m_decine_physique_et_r_adaptation" = "Médecine physique et réadaptation"
)
multiresponse_table(data, names(specialty_labels), specialty_labels,
                    "Répartition des spécialités demandées")
cat_table(data, "AUTRES_SPECIALITES", "Autres spécialités renseignées")
```

## 5. Prestations demandées

```{r prestations}
#| results: asis

prestation_labels <- c(
  "Prestation_/consultation" = "Consultation",
  "Prestation_/bilan_de_sant" = "Bilan de santé",
  "Prestation_/examen_paraclinique" = "Examen paraclinique",
  "Prestation_/chirurgie_ou_acte" = "Chirurgie ou acte"
)
multiresponse_table(data, names(prestation_labels), prestation_labels,
                    "Répartition des prestations demandées")
```

## 6. Sérologies jointes en cas de chirurgie

```{r serologies}
#| results: asis

sero_labels <- c(
  "Si_chirugie_s_rolog_ux_semaines_jointes_/vih" = "VIH",
  "Si_chirugie_s_rolog_ux_semaines_jointes_/syphilis" = "Syphilis",
  "Si_chirugie_s_rolog_ux_semaines_jointes_/h_patite_b" = "Hépatite B",
  "Si_chirugie_s_rolog_ux_semaines_jointes_/h_patite_c" = "Hépatite C"
)
# Le dénominateur reste l'ensemble des patients ; la faible complétude est donc visible.
multiresponse_table(data, names(sero_labels), sero_labels,
                    "Sérologies jointes renseignées")
```

## 7. Information, consentement et financement

```{r information-consent}
#| results: asis

cat_table(data, "PATIENT_RECU_INFO")
cat_table(data, "CONSENTEMENT")
cat_table(data, "PEUT_FINANCER_BILAN_PREOP")
num_table(data, "JOUR_PROGRAMME", "Description du jour programmé")
cat_table(data, "JOUR_PROGRAMME", "Répartition selon le jour programmé")
```

## 8. Complétude des variables d'analyse

```{r completeness}
#| results: asis

excluded <- c(
  "ID", "NOM", "PRENOMS", "TELEPHONE", "CONTACT_PROCHE",
  "Joindre_la_fiche_de_consentement_scann_e",
  "Joindre_la_fiche_de_consentement_scann_e_URL",
  "NOM_GRADE_ENREGISTREUR", "TELEPHONE_ENREGISTREUR",
  "_id", "_uuid", "_submission_time", "meta/rootUuid", "_index",
  "DATE_NAISSANCE_DATE", "DATE_RECENSEMENT_DATE"
)
analysis_vars <- setdiff(names(data), excluded)
comp <- data.frame(
  Variable = analysis_vars,
  Renseigné = sapply(data[analysis_vars], function(x) sum(!is.na(x) & trimws(as.character(x)) != "")),
  stringsAsFactors = FALSE
)
comp$Manquant <- nrow(data) - comp$Renseigné
comp$`Complétude (%)` <- paste0(format_number(100 * comp$Renseigné / nrow(data), 1), " %")
comp$Variable <- sapply(comp$Variable, get_label)
comp <- comp[order(comp$Renseigné, comp$Variable), ]
print_ft(style_ft(flextable(comp), first_col_width = 4.0))
```

## 9. Remarques descriptives sur la qualité des données

```{r data-quality}
#| results: asis

future_dates <- sum(data$DATE_RECENSEMENT_DATE > Sys.Date(), na.rm = TRUE)
missing_birth <- sum(is.na(data$DATE_NAISSANCE_DATE))
missing_age <- sum(is.na(data$AGE))
blank_consent_files <- if ("Joindre_la_fiche_de_consentement_scann_e" %in% names(data)) {
  sum(data$CONSENTEMENT == "Oui" & is.na(data$Joindre_la_fiche_de_consentement_scann_e), na.rm = TRUE)
} else NA

qualite <- data.frame(
  Contrôle = c(
    "Dates de recensement postérieures à la date de production du rapport",
    "Dates de naissance non interprétables / manquantes",
    "Âges non calculables après contrôle de plausibilité",
    "Consentements 'Oui' sans fichier de consentement joint"
  ),
  Effectif = c(future_dates, missing_birth, missing_age, blank_consent_files)
)
print_ft(style_ft(flextable(qualite), first_col_width = 5.0))
```

*Fin de l'analyse descriptive. Aucune analyse inférentielle, aucun test statistique et aucun modèle multivarié ne sont inclus dans ce script.*
